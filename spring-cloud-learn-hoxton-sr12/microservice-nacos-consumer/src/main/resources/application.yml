server:
  port: 80
spring:
  application:
    name: microservice-nacos-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
management:
  endpoints:
    web:
      exposure:
        include: "*"
service:
  url: http://microservice-nacos-provider
